# Contract test cache artifacts
data/_contract_cache_*/

# Local runtime env (operator-specific)
ai_workflow/.env.local

# Large local GWAS cache files (runtime)
data/gwas_cache/*.parquet
data/gwas_cache/*.meta.json
data/gwas_cache/.DS_Store

# Large local trait cache (generated at runtime)
data/trait_list.full.json
data/trait_list.full.meta.json

# Local runtime logs
logs/
